{
  "name": "orderfox-root",
  "version": "1.0.0",
  "description": "Root package for OrderFox application",
  "main": "index.js",
  "scripts": {
    "dev": "npx kill-port 3000 8000 && concurrently \"cd backend && python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000\" \"cd frontend_vanilla && npm run dev\"",
    "lint": "cd frontend_vanilla && npm run lint",
    "lint:fix": "cd frontend_vanilla && npm run lint:fix",
    "typecheck": "echo 'Use Pylance in VS Code for Python type checking'"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "kill-port": "^2.0.1"
  },
  "dependencies": {
    "ws": "^8.18.3"
  }
}
