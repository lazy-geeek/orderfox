# =============================================================================
# GLOBAL LOCAL ENVIRONMENT OVERRIDES (NOT COMMITTED TO VERSION CONTROL)
# Copy this file to .env.local and add your global overrides
# =============================================================================

# Global project overrides (if needed)
# NODE_ENV=production
# DEVCONTAINER_MODE=false

# Note: For Binance API keys, use backend/.env.local instead
# See backend/.env.local.example for backend-specific credentials